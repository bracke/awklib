# AGENTS.md

Agent guidance for this repository lives in [`CLAUDE.md`](./CLAUDE.md) — it is
tool-agnostic (build/test commands, the interpreter pipeline, conventions, and the
tri-platform CI). Read that file, and `src/awklib.ads` for the crate's own rationale.
